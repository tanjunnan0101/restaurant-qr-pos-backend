# ADR-001: TypeScript monorepo

Status: Accepted

Use npm workspaces with TypeScript across the API and shared packages. This keeps API contracts, database types, and future web applications aligned while allowing each package to build independently.
